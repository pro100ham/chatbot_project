version: "3.9"

services:
  fastapi:
    build: .
    container_name: chatbot-fastapi
    ports:
      - "8001:8000"
    env_file:
      - .env
    depends_on:
      - ollama
    networks:
      - chatbot-net

  ollama:
    image: ollama/ollama
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    networks:
      - chatbot-net

volumes:
  ollama-data:

networks:
  chatbot-net:
